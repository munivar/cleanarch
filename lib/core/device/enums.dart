enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum DevicesOrientation { isPortrait, isLandscape }

enum DeviceType {
  isSmall,
  isMedium,
  isBig,
  isTablet,
  isLaptop,
  isLapBig,
  is4K,
  isMini
}
